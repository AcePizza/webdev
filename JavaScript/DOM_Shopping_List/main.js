const uList = document.querySelector('ul');
const inputF = document.querySelector('input');
const subButton = document.querySelector('button');

function buttonPress() {
    const pressB = document.querySelector('button').click();
}